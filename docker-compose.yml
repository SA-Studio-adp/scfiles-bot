version: "3.8"

services:
  app:
    build: .
    container_name: sc_bot
    restart: unless-stopped
    env_file: .env
    ports:
      - "8080:8080"
    volumes:
      - ./logs:/app/logs
      - ./data:/app/data
